{% for category in categories_list %} 
    	<li><a href="{{ BASE_PATH }}{{ site.categories_path }}#{{ category[0] }}-ref">
    		{{ category[0] | join: "/" }}<span><sup>{{ category[1].size }}</sup></span>
    	</a></li>
    {% endfor %}
{% assign categories_list = nil %}